# Инструкции по деплою CRM Бота

## 1. Создание бота в BotFather

1. Откройте Telegram и найдите `@BotFather`
2. Отправьте `/newbot`
3. Введите имя бота (например: "Мой CRM Бот")
4. Введите username бота (должен заканчиваться на `bot`, например: `my_crm_bot`)
5. BotFather выдаст токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Сохраните этот токен** — это ваш `BOT_TOKEN`

## 2. Получение OpenAI API Key

1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел API Keys: https://platform.openai.com/api-keys
4. Нажмите "Create new secret key"
5. **Сохраните ключ** — это ваш `OPENAI_API_KEY`
6. Пополните баланс (минимум $5) в разделе Billing

## 3. Настройка Supabase

### 3.1 Создание проекта

1. Перейдите на https://supabase.com/
2. Зарегистрируйтесь или войдите
3. Нажмите "New Project"
4. Выберите организацию и введите название проекта
5. Установите пароль для базы данных (сохраните его!)
6. Выберите регион (ближайший к вам)
7. Нажмите "Create new project" и подождите создания

### 3.2 Получение ключей

1. В дашборде проекта перейдите в **Settings → API**
2. Скопируйте:
   - **Project URL** → это ваш `SUPABASE_URL`
   - **anon public key** → это ваш `SUPABASE_KEY`

### 3.3 Создание таблиц

1. Перейдите в **SQL Editor** в левом меню
2. Нажмите "New query"
3. Вставьте следующий SQL и нажмите "Run":

```sql
-- Leads table
create table leads (
    id bigint generated always as identity primary key,
    contact_telegram_id bigint,
    contact_name text,
    contact_username text,
    brand text,
    request text,
    dates text,
    status text default 'new',
    created_at timestamptz default now(),
    updated_at timestamptz default now()
);

-- Lead messages table
create table lead_messages (
    id bigint generated always as identity primary key,
    lead_id bigint not null references leads(id) on delete cascade,
    raw_text text not null,
    forward_date timestamptz,
    created_at timestamptz default now()
);

-- Indexes (performance optimization)
create index leads_status_idx on leads(status);
create index leads_contact_telegram_id_idx on leads(contact_telegram_id);
create index leads_updated_at_idx on leads(updated_at desc);
create index lead_messages_lead_id_idx on lead_messages(lead_id);
```

4. Должно появиться "Success. No rows returned" — таблицы созданы!

### 3.4 Проверка таблиц

1. Перейдите в **Table Editor** в левом меню
2. Вы должны видеть таблицы `leads` и `lead_messages`

## 4. Узнать свой OWNER_ID

1. Найдите бота `@userinfobot` в Telegram
2. Отправьте ему любое сообщение
3. Он ответит вашим ID — это число типа `55174158`
4. Это ваш `OWNER_ID`

## 5. Локальный запуск (для тестирования)

```bash
# Клонируйте или скопируйте проект
cd /path/to/CRMbot

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env
cp .env.example .env

# Отредактируйте .env и вставьте свои ключи
nano .env
# или
vim .env
```

Содержимое `.env`:
```
BOT_TOKEN=ваш_токен_от_botfather
OPENAI_API_KEY=ваш_ключ_openai
OWNER_ID=ваш_telegram_id

# Supabase
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJxxxxx...
```

```bash
# Запустите бота
python run.py
```

## 6. Тестирование

1. Откройте вашего бота в Telegram
2. Отправьте `/start` — должно появиться приветствие
3. Перешлите боту тестовое сообщение (из другого чата)
4. Подождите 5 секунд — бот должен создать лид
5. Проверьте кнопки смены статуса
6. Отправьте `/leads` — должен показать список лидов

### Тест батчинга (ВАЖНО!)

1. Быстро перешлите боту 4 сообщения подряд от одного человека
2. Подождите 5 секунд
3. Бот должен ответить **ОДИН раз** с информацией о лиде
4. В поле "Сообщений" должно быть "4"

### Проверка в Supabase

1. Откройте **Table Editor** в Supabase
2. Выберите таблицу `leads` — должен появиться созданный лид
3. Выберите таблицу `lead_messages` — должны быть все сообщения

## 7. Деплой на сервер (systemd)

```bash
# Скопируйте проект на сервер
scp -r CRMbot user@server:/home/user/

# Подключитесь к серверу
ssh user@server

# Установите Python 3.11+
sudo apt update
sudo apt install python3.11 python3.11-venv

# Перейдите в папку проекта
cd /home/user/CRMbot

# Создайте venv и установите зависимости
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создайте .env
cp .env.example .env
nano .env  # вставьте ключи

# Отредактируйте service файл
nano crm-bot.service
```

Замените в `crm-bot.service`:
- `YOUR_USERNAME` → ваш username на сервере
- `/path/to/CRMbot` → фактический путь (например `/home/user/CRMbot`)

```bash
# Скопируйте service файл
sudo cp crm-bot.service /etc/systemd/system/

# Перезагрузите systemd
sudo systemctl daemon-reload

# Запустите сервис
sudo systemctl start crm-bot

# Включите автозапуск
sudo systemctl enable crm-bot

# Проверьте статус
sudo systemctl status crm-bot

# Посмотрите логи
sudo journalctl -u crm-bot -f
```

## 8. Обновление бота

```bash
# Остановите бота
sudo systemctl stop crm-bot

# Обновите код
cd /home/user/CRMbot
git pull  # или скопируйте файлы заново

# Обновите зависимости (если изменились)
source venv/bin/activate
pip install -r requirements.txt

# Запустите бота
sudo systemctl start crm-bot
```

## Структура проекта

```
CRMbot/
├── src/
│   ├── __init__.py
│   ├── bot.py          # Основной код бота
│   ├── config.py       # Конфигурация
│   ├── database.py     # Работа с Supabase
│   ├── ai_parser.py    # AI парсинг
│   ├── keyboards.py    # Inline клавиатуры
│   └── formatters.py   # Форматирование сообщений
├── run.py              # Точка входа
├── requirements.txt    # Зависимости
├── .env.example        # Пример конфига
├── crm-bot.service     # Systemd сервис
└── DEPLOY.md           # Эта инструкция
```

## Возможные проблемы

### Бот не отвечает
- Проверьте `OWNER_ID` — бот отвечает только владельцу
- Проверьте токен бота
- Посмотрите логи: `sudo journalctl -u crm-bot -f`

### Ошибка OpenAI
- Проверьте баланс на https://platform.openai.com/usage
- Проверьте правильность API ключа
- GPT-4o-mini очень дешёвый, но баланс должен быть положительным

### Ошибка Supabase
- Проверьте `SUPABASE_URL` и `SUPABASE_KEY` в `.env`
- Убедитесь что таблицы созданы (SQL Editor → Run)
- Проверьте что используете **anon public key**, а не service_role

### Данные не сохраняются
- Откройте Table Editor в Supabase
- Проверьте что таблицы `leads` и `lead_messages` существуют
- Посмотрите логи бота на наличие ошибок

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие |
| `/leads` | Все лиды по статусам |
| `/search <запрос>` | Поиск по бренду/контакту |
| `/stats` | Статистика конверсии |

## Статусы лидов

| Emoji | Статус | Значение |
|-------|--------|----------|
| 🆕 | new | Только получен |
| 📤 | replied | Ждём их ответа |
| ⏳ | waiting | Они думают |
| 🤝 | negotiating | Обсуждаем условия |
| 📝 | signing | Этап контракта |
| ✅ | contract | Активный клиент |
| ❌ | lost | Отказ/потеряно |

## Преимущества Supabase

- **Облачная БД** — данные не потеряются при перезапуске сервера
- **Веб-интерфейс** — удобный просмотр и редактирование данных
- **Бесплатный план** — 500MB хранилища, более чем достаточно для CRM
- **Автобэкапы** — ежедневные бэкапы на платных планах
- **Масштабирование** — легко перейти на более мощный план
